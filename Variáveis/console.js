const tableStudents = ['Samara', 'Lílyan', 'Malu', 'Carlos']

console.log("Olá, usuário!")
console.table(tableStudents)
console.error("Socorro, deu erro")
console.assert(tableStudents.length < 0, "Só aparece quando dá erro?")